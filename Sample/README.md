# Introduction
This is an empty sample project.
You can use it to exercise the refactorings, as it references the refactorings project.

# Usage
Just create any .cs file. For example:

```cs
public record Test
{

}
```

Then, put the cursor in the record and try to invoke quick fix. Refactorings should appear.

Please, do not commit .cs files in that project.
